# 💰 Настройка комиссии ORACUL

## Как получать комиссию с TON → SOL свапов

### 1️⃣ Получите свой TON адрес кошелька

Откройте **Tonkeeper** или любой другой TON кошелёк и скопируйте адрес.

Пример адреса:
```
EQD7xYFjXlw8nCqO3t5TKOtB4CZD3F8RBqHqZnUO9s1x2AbC
```

### 2️⃣ Добавьте в переменные окружения Vercel

Зайдите в настройки проекта на Vercel:
- **Settings** → **Environment Variables**

Добавьте следующие переменные:

| Variable Name      | Value                                          | Environment        |
|--------------------|------------------------------------------------|--------------------|
| `FEE_WALLET_TON`   | `EQD7xY...2AbC` (ваш TON адрес)               | Production, Preview, Development |
| `FEE_PERCENT`      | `0.5` (процент комиссии, например 0.5%)       | Production, Preview, Development |

### 3️⃣ Redeploy проекта

После добавления переменных окружения:
```bash
vercel --prod
```

### 4️⃣ Как работает комиссия

Когда пользователь делает свап **TON → SOL**:

1. Пользователь вводит сумму, например **10 TON**
2. Backend вычисляет комиссию: `10 TON * 0.5% = 0.05 TON`
3. Из суммы свапа вычитается комиссия:
   - **9.95 TON** идёт в Symbiosis для обмена на SOL
   - **0.05 TON** отправляется на ваш кошелёк `FEE_WALLET_TON`

### 5️⃣ Проверка комиссии

После деплоя проверьте что переменные загружены:
```bash
# В логах Vercel вы увидите:
[ORACUL] FEE_WALLET_TON: EQD7xY...2AbC
[ORACUL] FEE_PERCENT: 0.5%
```

Если видите предупреждение:
```
[ORACUL] FEE_WALLET_TON не задан - комиссия не будет взиматься
```
Значит переменная не установлена правильно.

### 6️⃣ Изменение процента комиссии

Чтобы изменить процент (например, на 1%):

1. Зайдите в Vercel → Environment Variables
2. Измените `FEE_PERCENT` с `0.5` на `1.0`
3. Redeploy: `vercel --prod`

### 📊 Отслеживание дохода

Все входящие комиссии можно отслеживать в:
- **Tonkeeper** → История транзакций
- **TON Explorer**: https://tonscan.org/address/YOUR_ADDRESS

### ⚠️ Важно

- **Не коммитьте** `.env` файл в git (уже в `.gitignore`)
- Храните TON адрес в **безопасности**
- Проверяйте баланс комиссий **регулярно**

### 🔧 Локальная разработка

Для тестирования локально создайте `.env`:
```bash
FEE_WALLET_TON=EQD7xYFjXlw8nCqO3t5TKOtB4CZD3F8RBqHqZnUO9s1x2AbC
FEE_PERCENT=0.5
GROQ_API_KEY=gsk_your_key_here
```

Затем:
```bash
npm start
```

---

✅ **Готово!** Теперь вы получаете 0.5% с каждого TON → SOL свапа на свой кошелёк.
